[phases.build]
cmds = [
  "pnpm install",
  "pnpm run build:vercel",
  "cd server && pnpm install && npx nest build && cp -r ../dist-web dist/ && echo 'Build completed with dist-web in dist/'"
]

[start]
cmd = "node server/dist/main"

[build.env]
CI = "true"
NODE_ENV = "production"
